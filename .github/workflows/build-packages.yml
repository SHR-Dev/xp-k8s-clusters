name: build-packages

on: 
  - push

jobs:
  build-aks:
    runs-on: ubuntu-latest
    container: bitnami/kubectl
    steps:
    - run: curl -sL https://raw.githubusercontent.com/crossplane/crossplane-cli/master/bootstrap.sh | bash
    - run: ls -a
    - run: ./kubectl-crossplane build configuration -f aks-environment